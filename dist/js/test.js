"use strict";
/*
const allLifts = {
  squatLift: parseInt(document.getElementById("squat-lift").value),
  squatRep: parseInt(document.getElementById("squat-reps").value),
  benchLift: parseInt(document.getElementById("bench-lift").value),
  benchRep: parseInt(document.getElementById("bench-reps").value),
  deadliftLift: parseInt(document.getElementById("deadlift-lift").value),
  deadliftRep: parseInt(document.getElementById("deadlift-reps").value),
  ohpLift: parseInt(document.getElementById("ohp-lift").value),
  ohpRep: parseInt(document.getElementById("ohp-reps").value),
  pullLift: parseInt(document.getElementById("pull-lift").value),
  pullRep: parseInt(document.getElementById("pull-reps").value),
};

const lifts = [];
const reps = [];
const len = Object.keys(allLifts).length;
const keys = Object.keys(allLifts);

for (let i = 0; i < len; i++) {
  if (i % 2 === 0) {
    reps.push(keys[i]);
  } else {
    lifts.push(keys[i]);
  }
}

// lifts.forEach((num1, index) => {
//   const num2 = reps[index];
//   Math.trunc(num1 * (1 + 0.0333 * num2));
// });

const oneRepPr = function () {
  for (let i = 0; i < lifts.length; i++) {
    for (let i = 0; i < reps.length; i++) {
      Math.trunc(lifts[i] * (1 + 0.0333 * reps[i]));
    }
  }
};
*/
